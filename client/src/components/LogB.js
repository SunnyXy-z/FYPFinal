import React from 'react';
import './LogB.css'; // Import the CSS file for styling

const LogB = () => {
    return (
        <div className="login-container">
            <h1>LOGIN</h1>
        </div>
    );
};

export default LogB;